# Development dependencies for RTI MedTech Reference Architecture
#
# Install with: pip install -r requirements-dev.txt
#
# Note: rti.connextdds is installed separately via RTI Connext DDS installer
# and is NOT available on PyPI.

# Runtime dependencies (inherited)
-r requirements.txt

# Testing
pytest>=7.0
pytest-cov>=4.0

# Code quality
ruff>=0.9.0
pre-commit>=3.0

# Security artifact scaffolding
Jinja2>=3.1
