# Generated by setup_threat_security.sh — re-run the script to regenerate.
# Source files (.sh, .cnf, .xml) are tracked; everything else is derived.

# Certificates, keys, and serial databases
*.pem
*.srl

# PKCS#7 signed permissions and governance
*.p7s

# OpenSSL CA database files (from `openssl ca`)
index.txt
index.txt.attr
index.txt.attr.old
index.txt.old
serial
serial.old
newcerts/

# Rogue CA generated artifacts
rogue_ca/private/

# Signed governance output
xml/signed/
