# ca.cnf — OpenSSL CA config for signing expired-certificate threat identities.
#
# Must be invoked from the modules/04-security-threat/security/ directory:
#   openssl ca -batch -config expired/ca.cnf -startdate ... -enddate ... \
#              -in <csr> -out <cert>
#
# Paths to index.txt / serial / newcerts/ are relative to security/ (the CWD).
# Required environment variable:
#   TRUSTED_CA_DIR  — path to system_arch/security (holds ca/CaIdentity.pem etc.)

[ ca ]
default_ca = CA_default

[ CA_default ]
database      = expired/index.txt
serial        = expired/serial
new_certs_dir = expired/newcerts
certificate   = $ENV::TRUSTED_CA_DIR/ca/CaIdentity.pem
private_key   = $ENV::TRUSTED_CA_DIR/ca/private/CaPrivateKey.pem
default_md    = sha256
policy        = policy_anything

[ policy_anything ]
countryName             = optional
stateOrProvinceName     = optional
localityName            = optional
organizationName        = optional
organizationalUnitName  = optional
commonName              = supplied
emailAddress            = optional
